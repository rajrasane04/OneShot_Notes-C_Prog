// Arrays

int arr[5] = {1, 2, 3, 4, 5};           // Declaration and initialization
// printf("First element: %d\n", arr[0]);      // Accessing elements

//---------------------------------------------------------------------------------------------------------------------------------------

// Multidimensional Arrays

int matrix[2][2] = {{1, 2}, {3, 4}};
// printf("Element at (1, 1): %d\n", matrix[1][1]);

//---------------------------------------------------------------------------------------------------------------------------------------

// Strings

char str[20] = "Hello";
// printf("String: %s\n", str);

//---------------------------------------------------------------------------------------------------------------------------------------

// String Functions

#include <string.h>

// int len = strlen(str);          // Length of string
// strcpy(str, "World");               // Copy string
// strcat(str, "!");                   // Concatenate string
// int cmp = strcmp(str, "World!");            // Compare strings

//---------------------------------------------------------------------------------------------------------------------------------------